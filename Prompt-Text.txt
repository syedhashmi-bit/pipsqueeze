Read CLAUDE.md, CONTEXT.md and MEMORY.md first.

Then make the following change:

Add a "LAN Access Mode" option to client creation with three choices:
- "Internet Only" → AllowedIPs = 0.0.0.0/0
- "LAN Only" → AllowedIPs = 192.168.88.0/24
- "Full Access" → AllowedIPs = 0.0.0.0/0, 192.168.88.0/24

Changes needed:

1. app.py — DB:
   - Add column `access_mode TEXT DEFAULT 'internet'` to the clients table
   - Add it to the init_db() migration block too

2. app.py — home() route:
   - Read access_mode from request.form (values: 'internet', 'lan', 'full')
   - Build AllowedIPs string based on it:
     internet → "0.0.0.0/0"
     lan      → "192.168.88.0/24"
     full     → "0.0.0.0/0, 192.168.88.0/24"
   - Pass it into add_client() and store it in the DB
   - Use it when generating the .conf file instead of hardcoded "0.0.0.0/0"
   - Also pass access_mode to mt.add_peer() as the allowed_address only affects the server side — the AllowedIPs in the .conf is client-side only, so no MikroTik change needed

3. app.py — clone_client() and regen_client():
   - Clone should inherit access_mode from the source client
   - Regen should read access_mode from DB and use it when regenerating the .conf

4. app.py — update_client():
   - Allow changing access_mode via the edit modal
   - When access_mode changes, regenerate the .conf and QR code with the new AllowedIPs
   - Update MikroTik is NOT needed (AllowedIPs in .conf is client-side only)

5. templates/index.html — create form:
   - Add a styled three-button selector (not a plain <select>) for access mode
   - Match the existing tactical dark NOC design (cyan borders, monospace labels)
   - Internet Only selected by default
   - Show a one-line description under the selector explaining what each mode does

6. templates/index.html — edit modal:
   - Add the same three-button selector so existing clients can have their mode changed
   - Populate it correctly when the modal opens (pass current access_mode to openEdit())

7. templates/index.html — client list table:
   - Show a small badge on each client row indicating their current access mode
   - Internet Only = cyan badge, LAN Only = yellow badge, Full Access = green badge

8. templates/portal.html:
   - Show the access mode on the client portal page so the user knows what they have access to

After making all changes:
- Restart the service: systemctl restart vpn-dashboard
- Check logs: journalctl -u vpn-dashboard -n 30 --no-pager
- Update MEMORY.md with what was changed